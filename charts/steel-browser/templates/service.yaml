{{- $root := . -}}
{{- range $componentName, $component := .Values.components }}
{{- if $component.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "steel-browser.componentFullname" (dict "root" $root "component" $componentName) }}
  labels:
    {{- include "steel-browser.labels" $root | nindent 4 }}
    app.kubernetes.io/component: {{ kebabcase $componentName | quote }}
  {{- with $root.Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ $root.Values.service.type }}
  ports:
    - name: http
      port: {{ $root.Values.service.port }}
      targetPort: http
      protocol: TCP
    {{- if $root.Values.cdp.enabled }}
    - name: cdp
      port: {{ $root.Values.cdp.port }}
      targetPort: cdp
      protocol: TCP
    {{- end }}
  selector:
    {{- include "steel-browser.componentSelectorLabels" (dict "root" $root "component" $componentName) | nindent 4 }}
{{- end }}
{{- end }}
